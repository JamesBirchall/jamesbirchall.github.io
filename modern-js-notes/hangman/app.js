// app.js
// Birchall 2020
// Hangman Game

const product = {
  name: "Book of Dust",
}

// hasOwnProperty (as opposed to inherited property via prototypal inheritance)
console.log(product.hasOwnProperty("price"))
